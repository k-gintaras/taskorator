import { NgModule } from '@angular/core';
import { VortexRoutingModule } from './vortex-routing.module';

@NgModule({
  declarations: [],
  imports: [VortexRoutingModule],
})
export class VortexModule {}
