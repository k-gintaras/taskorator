import { Injectable } from '@angular/core';
import { Task } from '../models/taskModelManager';

@Injectable({
  providedIn: 'root',
})
export class UrlHelperService {
  selectedOverlordUrlUpdate(superOverlord: Task) {
    throw new Error('Method not implemented.');
  }
}
