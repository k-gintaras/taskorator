import { Injectable } from '@angular/core';

@Injectable({
  providedIn: 'root',
})
export class TaskApiService {
  // constructor() {}
  // fetchTasks; // get all tasks, preferably from cache if such exists, so we dont bother server
  // createTask;
  // createTaskWithId; // return unique id that it got after creating it, similar to SQL
  // updateTask;
  // updateTasks;
  // deleteTask;
}
