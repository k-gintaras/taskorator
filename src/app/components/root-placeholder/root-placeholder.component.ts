import { Component } from '@angular/core';

/**
 * Empty placeholder component for root route.
 * The rootGuard will redirect before this ever renders.
 */
@Component({
  selector: 'app-root-placeholder',
  standalone: true,
  template: '',
})
export class RootPlaceholderComponent {}
